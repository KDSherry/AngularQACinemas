//ReadMe.tx

npm install -g @angular/cli
npm start 

should download all the modules

#To start up server
in AngularQACinemas either:
1) nodemon (or ./node_modules/.bin/nodemon) or
2) node app
Server running on localhost:3010

#To populate a database from JSON files
in AngularQACinemas :
node populatedb

NOTE  -currently loads movies and showings into the db
      -JSON files located in create-db-jsons
